# Lovable prompts — index

Reusable prompts the user can invoke by name. Each prompt is a standalone file in `.lovable/prompts/`.

| Trigger phrase | File | Purpose |
|---|---|---|
| `write memory` / `end memory` | [01-write-memory.md](./prompts/01-write-memory.md) | Persist everything learned/done/pending so the next session has zero context loss. Includes CI/CD issue tracking, plan/suggestions sync, index integrity validation. |
