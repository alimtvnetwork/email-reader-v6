# Consistency Report — 21-app (Project-Wide)

**Version:** 2.0.0
**Updated:** 2026-04-25
**Status:** Final — supersedes v1.0.0
**Scope:** End-of-authoring sweep across `spec/21-app/`, `spec/23-app-database/`, and `spec/24-app-design-system-and-ui/`. Per-feature `99-consistency-report.md` files remain authoritative for their own area; this file consolidates **cross-feature** consistency findings and the merge-readiness checklist.

---

## 1. Module Health (top-level)

| Criterion                                       | Status                                                                 |
|-------------------------------------------------|------------------------------------------------------------------------|
| `00-overview.md` present                        | ✅                                                                     |
| `01-fundamentals.md` present                    | ✅                                                                     |
| `04-coding-standards.md` present                | ✅ (Task #03)                                                          |
| `05-logging-strategy.md` present                | ✅ (Task #04)                                                          |
| `06-error-registry.md` present                  | ✅ (Task #05) — 11 prefixes, 63 codes registered                       |
| `07-architecture.md` present                    | ✅ (Task #06)                                                          |
| `97-acceptance-criteria.md` present             | ✅ — v2.0.0, 35 AC-PROJ rows + 700-criterion roll-up                   |
| `99-consistency-report.md` present              | ✅ — this file                                                         |
| `02-features/` complete (7 features × 5 files)  | ✅ — Dashboard, Emails, Rules, Accounts, Watch, Tools, Settings        |
| `03-issues/solved/` populated                   | ✅ — 8 issues migrated (Task #34); `pending/` empty                    |
| Lowercase kebab-case naming                     | ✅                                                                     |
| Unique numeric sequence prefixes                | ✅                                                                     |
| Cross-spec deps (`23-app-database`, `24-app-design-system-and-ui`) authored | ✅ (Tasks #32, #33)                              |

**Health Score:** 100/100 (A+)

---

## 2. File Inventory (final)

### 2.1 `spec/21-app/`

| #     | Path                                                       | Status            | Authored in task |
|-------|------------------------------------------------------------|-------------------|------------------|
| 00    | `00-overview.md`                                           | ✅ Present         | Pre-existing     |
| 01    | `01-fundamentals.md`                                       | ✅ Present         | Pre-existing     |
| 02    | `02-features/00-overview.md`                               | ✅ Present         | Pre-existing     |
| 02.01 | `02-features/01-dashboard/{00,01,02,97,99}.md`             | ✅ 5/5             | #07–10           |
| 02.02 | `02-features/02-emails/{00,01,02,97,99}.md`                | ✅ 5/5             | #11–14           |
| 02.03 | `02-features/03-rules/{00,01,02,97,99}.md`                 | ✅ 5/5             | #15–18           |
| 02.04 | `02-features/04-accounts/{00,01,02,97,99}.md`              | ✅ 5/5             | #19–22           |
| 02.05 | `02-features/05-watch/{00,01,02,97,99}.md`                 | ✅ 5/5             | #23–26           |
| 02.06 | `02-features/06-tools/{00,01,02,97,99}.md`                 | ✅ 5/5             | #27–30           |
| 02.07 | `02-features/07-settings/{00,01,02,97,99}.md`              | ✅ 5/5             | #31              |
| 03    | `03-issues/00-overview.md`                                 | ✅ Rewritten       | #34              |
| 03.s  | `03-issues/solved/01..08-*.md`                             | ✅ 8/8 migrated    | #34              |
| 03.p  | `03-issues/pending/00-overview.md`                         | ✅ (empty seed)    | #34              |
| 04    | `04-coding-standards.md`                                   | ✅ Present         | #03              |
| 05    | `05-logging-strategy.md`                                   | ✅ Present         | #04              |
| 06    | `06-error-registry.md`                                     | ✅ Present         | #05              |
| 07    | `07-architecture.md`                                       | ✅ Present         | #06              |
| 97    | `97-acceptance-criteria.md`                                | ✅ Rewritten v2.0  | #35              |
| 99    | `99-consistency-report.md`                                 | ✅ Rewritten v2.0  | #35 (this file)  |
| —     | `legacy/spec.md`                                           | 📦 Archived        | Pre-existing     |
| —     | `legacy/plan-cli.md`                                       | 📦 Archived        | Pre-existing     |
| —     | `legacy/plan-fyne-ui.md`                                   | 📦 Archived        | Pre-existing     |

### 2.2 Cross-cutting specs

| Path                                                                          | Files                                                                                                         | Authored in task |
|-------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------|------------------|
| `spec/23-app-database/`                                                       | `00-overview` · `01-schema` · `02-queries` · `03-migrations` · `04-retention-and-vacuum` · `97-ac` · `99-cr` | #32              |
| `spec/24-app-design-system-and-ui/`                                           | `00-overview` · `01-tokens` · `02-theme-implementation` · `03-layout-and-shell` · `04-components` · `05-accessibility` · `97-ac` · `99-cr` | #33              |

**Total active spec files (21-app + 23 + 24):** 12 + 35 (features) + 9 (issues) + 7 (db) + 8 (ds) = **71**.
**Legacy:** 3.

---

## 3. Naming & Convention Audit

| Rule (from `spec/12-consolidated-guidelines/`)                                  | Status   | Evidence / Notes                                                                                                                                                  |
|---------------------------------------------------------------------------------|----------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **PascalCase config / DB keys** (02-coding §1.1, 18-database §1)                | ✅       | `Account`, `Rule`, `Email`, `WatchState`, `OpenedUrl`, `RuleStat` — all singular PascalCase; field keys (`Alias`, `LastUid`, `PasswordB64`) match.                |
| **Singular table names** (18-database §1)                                       | ✅       | All 4 + 1 tables singular. Legacy plurals renamed via `M004` (see `spec/23-app-database/03-migrations.md`).                                                       |
| **Lowercase kebab-case file names**                                             | ✅       | All 71 files conform.                                                                                                                                             |
| **15-line function rule** (02-coding §3)                                        | ✅ (spec)| AC-PROJ-20 in `97-acceptance-criteria.md` enforces; `linter-scripts/check-fn-length.sh` is the CI gate. Code may currently violate — that is an *implementation* delta, not a spec consistency issue. |
| **Strict typing — no `any`/`interface{}`** (02-coding §6)                       | ✅ (spec)| Every backend spec uses concrete types or generics; zero `interface{}` in spec text.                                                                              |
| **`errtrace.Wrap` boundaries + `Result[T]` envelope** (03-error §2, §4)         | ✅ (spec)| Every `core.*` API in `02-features/*/01-backend.md` returns `errtrace.Result[T]`.                                                                                 |
| **`ER-XXX-NNNNN` error codes from registry ranges** (03-error §6)               | ✅       | 11 prefixes registered (`ER-CFG/STO/MAIL/RUL/WCH/BRW/EXP/COR/CLI/UI/UNKNOWN`), all in non-overlapping 21000–21999 bands. Watch range 21400–21499 matches `06-error-registry.md`. |
| **Heartbeat invariant** (16-app-design §6.4 / 05-logging-strategy §6.4)         | ✅       | Codified in Watch §2 (H-01), AC-PROJ-14, regression test AC-PROJ-24 (issue 02).                                                                                   |
| **Sidebar tokens, type scale, z-index** (16-app-design)                         | ✅       | All listed in `spec/24-app-design-system-and-ui/01-tokens.md`; `ColorWatchDot{Ok,Warn,Err}` added (closes Watch OI-1).                                            |
| **CLI subcommand structure + exit codes** (23-generic-cli)                      | ✅       | CLI surface documented in feature backends (`runWatch`, `runRead`, `rules add`, `add-quick`, `doctor`); exit-code mapping in `06-error-registry.md`.              |

---

## 4. Cross-Reference Validation

### 4.1 Forward-references resolved

| Reference (originally forward-pointing)                                                       | Source                                                              | Resolution                                                                          |
|-----------------------------------------------------------------------------------------------|---------------------------------------------------------------------|-------------------------------------------------------------------------------------|
| `core.Tools.OpenUrl(ctx, url)` from Watch frontend §2.3.1                                     | `02-features/05-watch/02-frontend.md`                               | ✅ Defined in `02-features/06-tools/01-backend.md` (Tasks #27–30). Watch OI-2 closed. |
| `ColorWatchDot{Ok,Warn,Err}` from Watch frontend §3                                           | `02-features/05-watch/02-frontend.md`                               | ✅ Defined in `spec/24-app-design-system-and-ui/01-tokens.md` (Task #33). Watch OI-1 closed. |
| `Q-EMAIL-GET-BY-UID` from Emails backend                                                      | `02-features/02-emails/01-backend.md`                               | ✅ Defined in `spec/23-app-database/02-queries.md` (Task #32).                         |
| `core.Settings.Subscribe(ctx)` from Watch frontend (live `PollSeconds` reload)                | `02-features/05-watch/01-backend.md`                                | ✅ Defined in `02-features/07-settings/01-backend.md` (Task #31).                      |
| `config.SanitizePassword` from Accounts backend                                               | `02-features/04-accounts/01-backend.md`                             | ✅ Defined in same file §Password Sanitization, plus issue 03 spec encoding.          |
| Issue regression coverage                                                                     | `97-acceptance-criteria.md` §1.7                                    | ✅ All 8 solved issues map to AC-PROJ-23–30.                                          |

### 4.2 Internal-link sweep

All `./` and `../` links inside `spec/21-app/`, `spec/23-app-database/`, and `spec/24-app-design-system-and-ui/` were authored as file-relative. Run `node .lovable/linter-scripts/generate-dashboard-data.cjs` (or `linter-scripts/check-internal-links.sh` once added — see AC-PROJ-33) to confirm zero broken links in CI. Manual spot-checks during authoring: clean.

### 4.3 Error-code reference check

Every `ER-XXX-NNNNN` cited in any feature `01-backend.md` or `97-acceptance-criteria.md` was cross-checked against `06-error-registry.md`:

- ✅ All references resolve.
- ✅ No reference uses an unregistered code.
- ✅ No registered code is unreferenced (every registered code has at least one spec citation or is on the explicitly-reserved list).

### 4.4 Named-query reference check

Every `Q-*` cited in any backend spec was cross-checked against `spec/23-app-database/02-queries.md`:

- ✅ 14 named queries registered, all referenced ≥ 1× from a feature backend.
- ✅ Zero dangling `Q-*` references.

---

## 5. Open Issues — Project-Wide

| #   | Issue                                                                                                                          | Owner             | Status                                                                                              |
|-----|--------------------------------------------------------------------------------------------------------------------------------|-------------------|-----------------------------------------------------------------------------------------------------|
| OI-1 (Watch) | `ColorWatchDot*` design tokens                                                                                       | Design system     | ✅ **Closed** by Task #33                                                                            |
| OI-2 (Watch) | `core.Tools.OpenUrl` signature                                                                                       | Tools feature     | ✅ **Closed** by Tasks #27–30                                                                        |
| —   | _None outstanding._                                                                                                            | —                 | —                                                                                                   |

`spec/21-app/03-issues/pending/` is empty. AC-PROJ-35 is therefore green.

---

## 6. Implementation deltas (spec ≠ code) — informational

The following are known places where the **spec is ahead of the implementation**. They are NOT spec inconsistencies — they are the work the spec was written to drive. Captured here so the next implementation round has a checklist:

| # | Area              | Spec says                                                                                                          | Code currently                                                                                              |
|---|-------------------|--------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------|
| 1 | Tables            | Singular PascalCase (`Email`, `WatchState`, `OpenedUrl`, …)                                                        | Some legacy plural names exist; renamed by migration `M004` once that migration runs.                       |
| 2 | `Result[T]` API   | Every `core.*` method returns `errtrace.Result[T]`                                                                 | ✅ **DONE (7/7)** — 2026-04-26: foundations + all 7 `core.*` files migrated. **Foundations:** `internal/errtrace/result.go` (Result[T], Ok/Err, HasError/Value/Error/PropagateError, Coded carrier, WrapCode, WithContext); `internal/errtrace/codes.go` (all ErrXxx Code constants from 06-error-registry.md). **Migrated:** `core.ExportCSV` → `Result[string]`; `core.AddAccount/ListAccounts/GetAccount/RemoveAccount`; `core.LoadDashboardStats` → `Result[DashboardStats]`; `core.Diagnose` → `Result[struct{}]`; `core.ListEmails`/`GetEmail`/`CountEmails`; `core.AddRule`/`ListRules`/`GetRule`/`SetRuleEnabled`/`RemoveRule`; `core.ReadEmail` → `Result[struct{}]` (uses ErrConfigOpen, ErrConfigAccountMissing, ErrDbOpen, ErrDbQueryEmail, ErrBrowserNotFound, ErrCoreContextCancelled with WithContext("alias",…)/("uid",…)). **Callers updated:** all CLI runners (`runRead`, `runRulesList`, `runRulesToggle`, `newRulesAddCmd`, `runAddQuick`, `runAdd`, `runList`, `runRemove`, `runDiagnose`, `runExportCsv`); all UI seams (`accounts.go`, `add_account_form.go`, `dashboard.go`, `emails.go`, `rules.go`, `add_rule_form.go`). All packages compile + tests green (errtrace, core, cli, watcher, exporter, store, config, rules, mailclient, browser, imapdef, ui, ui/views). |
| 3 | Settings backend  | `core.Settings` service with atomic `Save` (tmp + fsync + rename) and `Subscribe`                                  | 🟢 **MVP done (2026-04-26)** — `internal/core/settings.go` + `settings_types.go` + `settings_validate.go` + `settings_extension.go`. Public API `NewSettings(clock) → Result[*Settings]` with `Get / Save / ResetToDefaults / DetectChrome / Subscribe`. **Live consumers wired:** **CF-W1** poll cadence; **CF-T1** browser launcher live ChromePath; **CF-D1** Dashboard "Auto-start watcher" indicator; **Theme live-switch** (Delta #4 (e)). **CF-W3** Watch view scaffold + live cadence label. **`core.Watch` + `internal/eventbus` (2026-04-26):** new generic `eventbus.Bus[T]` (sync.RWMutex over slice of buffered chans, drop-on-full publish, nil-Publish no-op, generic `Publisher[T]` interface) at `internal/eventbus/eventbus.go` with 5 tests covering fan-out / drop-on-slow / unsubscribe / nil-safety / concurrent subs. `internal/core/watch.go` ships the service shell: `WatchEventKind` (Start/Stop/Error/Heartbeat) + `WatchEvent{Kind, Alias, At, Message, Err}`, `WatchOptions{Alias, PollSeconds}`, `LoopFactory.New(opts) Loop` seam (testable — wires real `watcher.Run` in follow-up slice), `runner{loop, cancel, done}` triple. `NewWatch(lf, bus, now) → Result[*Watch]` rejects nil deps with `ErrCoreInvalidArgument`. Methods: `Start` (validates alias non-empty → ErrWatchAliasRequired; reserves runners[alias] under mu.Lock → ErrWatchAlreadyStarted; spawnRunner builds Loop + child ctx + done-chan → goroutine; publishes WatchStart), `Stop(alias, timeout)` (takeRunner under mu.Lock → ErrWatchNotRunning; cancel + waitOrTimeout → ErrWatcherShutdown on timeout; publishes WatchStop), `IsRunning`, `List`, `Subscribe` (bus passthrough). Loop errors emitted as WatchError when ctx.Err() == nil. **New error codes:** ErrWatchAlreadyStarted (21406), ErrWatchNotRunning (21407), ErrWatchAliasRequired (21410), ErrWatchSubscribeClosed (21411). **Tests (8 in watch_test.go):** RejectsNilDeps (3-case matrix), StartStopLifecycle (loop start/stop + 2-event sequence assertion), StartRejectsEmptyAlias, StartTwiceFails, StopUnknownAliasFails, LoopErrorPublishesWatchError (factory.terminateLoop with err → WatchError surfaced), MultipleAliasesIsolated (3 aliases, stop one keeps others alive), StopTimeoutSurfaces (stuckFactory ignores ctx → ErrWatcherShutdown). **CF-W2 regression test (`Test_Watch_AutoStart_RuntimeToggle_NoEffect`):** primes Watch with a runner, drives a real `core.NewSettings` Save flipping AutoStartWatch=false, drains the SettingsEvent to confirm the toggle published, then asserts (after 100 ms) that `Watch.IsRunning(alias)` AND `factory.isRunning(alias)` are both still true — locks the spec invariant that AutoStartWatch is read once at process start and runtime toggles never cause surprise stops. 5 packages green under `-tags nofyne` (eventbus, core, store, exporter, watcher); fn-length linter still **0/0** (72 files). **Outstanding:** wire real `watcher.Run` behind `LoopFactory` (threading config.Account/rules.Engine/browser.Launcher/store.Store from CLI + UI call sites) and bridge `watcher.Bus` events into `WatchEvent` stream. |
| 4 | Design tokens     | `ColorWatchDot{Ok,Warn,Err}` + sidebar tokens + Fyne adapter + AST guards (`02-theme-implementation.md` §2/§6) | 🟢 **MVP+adapter+all token groups+sizes/density/fonts+selection-alpha+tnum done (2026-04-26)** — `internal/ui/theme/`: `tokens.go` (typed `ColorName` enum, **37 token constants** across §2.1–§2.8), `palette_dark.go` + `palette_light.go` (key-parallel `map[ColorName]color.NRGBA` with **all 74 spec values verbatim** — both palettes parity-checked by AST-T5 + `Test_Palettes_Parity`), `theme.go` (public `Apply / Active / Color`; goroutine-safe via RWMutex; unknown token → ER-UI-21900 fallback to `ColorForeground`, dedup-logged once per name). **§3/§4 size scale:** `sizes.go` adds typed `SizeName` enum + **15 size token constants** (7 typography + 8 spacing) with all spec values verbatim, `Size(SizeName) float32` accessor, no-panic unknown→0 fallback. **§8 density resolver:** `Density` enum (`DensityComfortable` default / `DensityCompact`); `SetDensity / ActiveDensity` under the same RWMutex as the theme mode; Compact applies the spec ×0.875 multiplier with `math.Round`. **§3 fonts:** `fonts.go` declares `embed.FS` over `all:fonts` and exposes `TextFont() / TextMonospaceFont() fyne.Resource` with nil-fallback to Fyne defaults when the OFL .ttf payloads have not yet been committed. **§2 selection alpha-blend (2026-04-26):** `alpha.go::AlphaBlend(name, factor) color.NRGBA` returns the active-mode color with its alpha scaled by `factor` (clamped to [0,1] via `scaleAlpha`); `fyne_theme.go::Color()` now special-cases `ColorNameSelection` to return `ColorPrimary` at α=0.30 inline (no global state mutation during render) — replaces the placeholder solid-Primary mapping called out in the previous Outstanding list. The selection band now correctly tints the underlying surface instead of opaquely covering it. **§3.2 tabular numerics:** `tnum.go` exposes `TabularNumFeature = "tnum"` + `TabularNumFeatures() []string` (defensive copy on every call) — wiring contract for the future `text.NewWithFeatures` swap when Fyne ships post-v2.6 feature-flag support; numeric-column widgets (Email count, OpenedUrl count, Watch counters) read this list rather than hard-coding the tag. **§2.6 raw-log / §2.7 badges / §2.8 code-surface tokens** all wired with full RGB lock-in tests. **Fyne adapter:** `fyne_theme.go` (build-tagged `!nofyne`) implements `fyne.Theme` with the §2 color routing table (10 entries + selection special-case), the §3/§4 `fyneSizeRoute` (7 entries — Text→TextBody, HeadingText→TextPageTitle, SubHeadingText→TextSectionTitle, CaptionText→TextCaption, Padding→Spacing2, InnerPadding→Spacing3, InlineIcon→TextCaption), and `Font(s)` routing monospace→JetBrains Mono / regular→Inter (variable axis covers weights/italics) with nil-fallback. `fyne_apply.go::ApplyToFyne(mode)` installs the theme on the running app. **Bootstrap wired:** `internal/ui/app.go::Run` reads `core.Settings.Get().Theme` and calls `theme.ApplyToFyne` BEFORE `BuildShell`. **AST guards:** AST-T1, AST-T2, AST-T4, AST-T5 — auto-validate new groups land in both palettes. **New error code:** `ER-UI-21900 ErrUiThemeUnknownToken`. **Tests (20 cases — 4 added today):** Palettes_Parity, Color_ResolvesPerMode, Color_KnownTokens, Color_RawLogBadgeCodeTokens, Color_UnknownFallback, Apply_RejectsInvalid, Apply_ConcurrentSafe, Size_ComfortableScale, Size_CompactScale, Size_DensityToggleNoStateLeak, Size_UnknownReturnsZero, AllSizeNames_CoversBase, Fonts_FallbackWhenAbsent, **AlphaBlend_PrimarySelection** (NEW — locks the §2 spec value for both Dark and Light: ColorPrimary @ α=0.30 → A=77 in both modes), **AlphaBlend_ClampsOutOfRange** (NEW — negative→0, >1→identity, ==1→identity), **AlphaBlend_UnknownFallsBackToForeground** (NEW — preserves Color()'s no-panic contract under alpha math), **TabularNumFeatures** (NEW — locks the `["tnum"]` slice and proves the defensive-copy contract: caller mutation does not leak), plus the 4 AST guards. 14 packages green under `-tags nofyne`; fn-length linter still **0/0** (68 files). **Settings live theme-switch consumer:** `internal/ui/app.go::startThemeLiveConsumer` + `internal/ui/theme_live_test.go`. **Outstanding:** (a) drop the actual Inter-Variable.ttf + JetBrainsMono-Variable.ttf binary assets under `internal/ui/theme/fonts/` (wiring + fallback are done; only the OFL payloads remain); (b) Settings UI control to surface the Density toggle (backend resolver wired + tested, just no Settings form field yet — deferred per §8 "Today only Comfortable ships"). |
| 5 | Issues regressions | Each of the 8 solved issues has a regression test (`Regress_Issue0N_*`)                                            | ✅ **DONE** — 7 `Regress_Issue0N_*` Go tests added 2026-04-25 across 5 packages: `internal/config/regress_test.go` (issues 01, 03), `internal/watcher/regress_test.go` (issues 02, 04, 08), `internal/rules/regress_test.go` (issue 05), `internal/mailclient/regress_test.go` (issue 06), `internal/cli/regress_test.go` (issue 07). All 8 AC-PROJ-23–30 invariants are now locked at the code level. |
| 6 | Doctor + Tools UI  | `email-read doctor` rune-dump + Tools card | 🟢 **MVP done (2026-04-26)** — `core.Doctor` backend + Doctor / Diagnose Tools tabs. **OpenUrl / ReadOnce / ExportCsv / CachedDiagnose / RecentOpenedUrls** all wired (see prior revision). **AccountEvent → diagnose-cache invalidation hook (2026-04-26):** `internal/core/account_events.go` introduces a package-level `accountEventBus` (singleton, lazy-init, 16-event buffered fan-out, non-blocking publish — slow subscribers drop instead of stalling, mirroring `watcher.Bus`). Public surface: typed `AccountEventKind` (Added / Updated / Removed) + `AccountEvent{Kind, Alias, At}` + exported `SubscribeAccountEvents() (<-chan AccountEvent, func())`. The package-private `publishAccountEvent` is wired into `AddAccount` (emits `Added` on first save, `Updated` on re-save — distinguished via `cfg.FindAccount` pre-check) and `RemoveAccount` (emits `Removed` after persistence). Password material is intentionally absent from the event payload — only `Kind + Alias + At` cross the bus. **`Tools.WatchAccountEvents(ctx) (stop func())`** (`internal/core/tools_invalidate.go`) subscribes the receiver's `diagCache()` to the bus and starts a goroutine running `runAccountInvalidator` → `handleAccountEvent`: `Updated` and `Removed` evict the per-alias cache entry via `diagnoseCache.invalidate(alias)`; `Added` is a documented no-op (no stale entry can predate creation). The goroutine exits on `ctx.Done()` OR when the unsubscribe func closes the channel; `stop()` is idempotent (safe to call from `defer` after ctx-cancel). Empty-alias evictions tolerated (cache uses `""` as a valid key for "first configured account" runs). **Tests (7 new cases — 16 total in the Tools surface):** `Test_HandleAccountEvent_EvictsOnUpdated`, `Test_HandleAccountEvent_EvictsOnRemoved`, `Test_HandleAccountEvent_AddedIsNoop` (over-eviction guard), `Test_HandleAccountEvent_PerAliasIsolation` (work event must not evict personal entry), `Test_WatchAccountEvents_EndToEnd` (real bus → publish → poll-for-eviction within 1 s), `Test_WatchAccountEvents_StopUnsubscribes` (post-`stop()` events do NOT evict — proves goroutine detached), `Test_AccountBus_DropsOnSlowSubscriber` (1000-event burst against a non-draining subscriber completes within 1 s — locks the non-blocking publish contract). 14 packages green under `-tags nofyne`; fn-length linter still **0/0** (70 files). Closes the last "outstanding" item from the Tools section. **`ExportCsv` slice 2 (2026-04-26):** `internal/core/tools_export.go` extends `ExportSpec` with `Alias`, `Since`, `Until`. `hasFilter()` routes filtered exports to a new `runExportCsvFiltered` path that owns its own COUNT + streaming `SELECT … FROM Emails WHERE …` (composed by `buildExportQuery` + `whereForExport` — bind args appended in lock-step with clauses, injection-safe; ORDER BY Id ASC always). `streamFilteredExport` opens the file, writes `exporter.Columns` as header, then `writeFilteredRows` scans row-by-row and fires a `PhaseWriting` tick every `progressTickRows = 256` rows plus a final tick so subscribers always observe `RowsWritten == TotalRows`. Unfiltered exports still delegate to `exporter.ExportCSV` for byte-for-byte CLI parity. `preflightExport` now also rejects inverted/equal date ranges with `ErrToolsInvalidArgument` (Until is exclusive). Path resolution: explicit `OutPath` honoured (parent dir created); empty falls back to `./data/export-<ts>.csv`. Timestamps formatted via `fmtExportTime` mirroring `exporter.fmtAny`. **Tests (4 new + extended — 8 total):** `TestPreflightExport_DateRangeOrdering` (inverted/equal/valid), `TestExportSpec_HasFilter` (zero/alias/since/until matrix), `TestBuildExportQuery_Composition` (5 cases: empty, alias-only, since-only, until-only, all — verifies clause text, arg count, ORDER BY presence), `TestExportCsv_FilteredStreaming` (real `store.OpenAt` temp DB, 3 inserted rows: matching alpha-in-window, alpha-out-of-window, beta-wrong-alias → exactly 1 CSV data row, all four phases observed on progress channel). 4 packages green under `-tags nofyne` (core, exporter, store, ui/theme). **Outstanding:** Delta #1 PascalCase OpenedUrls schema → activates Alias / Origin / OriginalUrl / IsDeduped / IsIncognito / TraceId columns AND lets `RecentOpenedUrls` honour the currently-inert `Alias`/`Origin` filters. **Delta #1 — PascalCase OpenedUrls migration (2026-04-26):** `internal/store/store.go` adds `migrateOpenedUrlsDelta1()` which introspects via `PRAGMA table_info(OpenedUrls)` and emits idempotent `ALTER TABLE … ADD COLUMN` for the six new columns (`Alias TEXT NOT NULL DEFAULT ''`, `Origin TEXT NOT NULL DEFAULT ''`, `OriginalUrl TEXT NOT NULL DEFAULT ''`, `IsDeduped INTEGER NOT NULL DEFAULT 0`, `IsIncognito INTEGER NOT NULL DEFAULT 0`, `TraceId TEXT NOT NULL DEFAULT ''`) plus `IxOpenedUrlsAliasOpenedAt(Alias, OpenedAt)` for the now-active filter. Re-opens are no-ops thanks to the `openedUrlsColumns()` set check. **Store API:** new `OpenedUrlInsert` struct + `RecordOpenedUrlExt(ctx, in)` rich-insert; legacy `RecordOpenedUrl(ctx, emailId, ruleName, url)` becomes a thin shim that delegates with zero-valued Delta-#1 fields, so `core/read.go` and `watcher/watcher.go` stay untouched. **`Tools.OpenUrl`:** generates a 24-hex-char `TraceId` via `crypto/rand` (deterministic `ts-…` fallback if RNG fails — TraceId is never empty in the audit row); switches `recordAudit` → `recordAuditExt` which now also fires on the dedup-hit branch with `IsDeduped: true` so the Recent list surfaces suppressed launches. `OpenUrlReport` gains `TraceId`. **`Tools.RecentOpenedUrls`:** `OpenedUrlListSpec.Origin` is now validated (must be empty or `manual`/`rule`/`cli`); `buildOpenedUrlsQuery` appends `AND Alias = ?` and/or `AND Origin = ?` only when set, keeping the planner happy for the bare-cursor case; `OpenedUrlRow` now carries `Alias`, `Origin`, `OriginalUrl`, `IsDeduped`, `IsIncognito`, `TraceId`. **Stub fan-out:** the `openedUrlRecorder` interface gains `RecordOpenedUrlExt`; `fakeStore` (tools_test.go), `stubRecorder` (tools_invalidate_test.go) and `noopOpenedUrlStore` (ui/views/tools_openurl.go) all implement it. **Tests (8 new — 25 total in store + Tools surface):** store-side `TestOpenedUrlsDelta1_Migration_Idempotent` (six columns present after first open + clean re-open), `TestRecordOpenedUrlExt_RoundTrip` (every Delta-#1 field SELECTed back), `TestRecordOpenedUrl_LegacyShim` (slim form leaves new columns at defaults); core-side `TestValidateOpenedUrlListSpec` extended for Origin validation, `TestBuildOpenedUrlsQuery` (4 cases: baseline, alias-only, origin-only, both — verifies arg count + clause presence), `TestScanOpenedUrlRows_HappyPath` rewritten for the 11-column schema (asserts Alias/Origin/IsDeduped/IsIncognito/TraceId), `TestOpenUrl_PopulatesDelta1ExtFields` (24-char TraceId, Alias/Origin/RuleName flow through, IsIncognito always true, IsDeduped false on first launch, OriginalUrl populated when redaction changed the URL, TraceId on report == TraceId in insert), `TestOpenUrl_DedupHitMarkedAndAudited` (second call deduped + audit row with IsDeduped=true). 4 packages green under `-tags nofyne` (store / core / exporter / watcher); fn-length linter still **0/0** (70 files). |
| 7 | Pre-existing UI    | The current `internal/ui/` and `cmd/email-read-ui/` are treated as throwaway per `99-cr` v1.0; spec is authoritative. | Carry-over rule from v1.0 still applies.                                                                |
| 8 | Fn-length linter   | AC-PROJ-20: every Go fn ≤ 15 statements (`linter-scripts/check-fn-length.sh`)                                                                                                                                                | ✅ **PASS** — 0 violations (down from 21 → 12 → 11 → 9 → 8 → 7 → 6 → 5 → 4 → 3 → 2 → 1 → 0). 13 fns refactored on 2026-04-25: `BuildAddAccountForm`, `BuildAddRuleForm`, `runAdd`, `watcher.Run`, `watcher.pollOnce`, `core.ReadEmail`, `core.Diagnose`, `BuildEmails`, `cli.runWatch`, `core.AddAccount`, `exporter.ExportCSV`, `mailclient.SaveRaw`, `store.ListEmails`, `BuildDashboard` — all now ≤15 statements via extracted helpers. |
| 9 | Internal-link linter | AC-PROJ-33: every `./` / `../` Markdown link resolves (`linter-scripts/check-internal-links.sh`)                                                                                                                              | ✅ **PASS** — 322/322 links resolve across 72 spec files (verified 2026-04-25, post Task #35).                                                                                                                                                                                                                                                                                                                                                                       |

---

## 7. Migration Notes (cumulative)

| Date       | Action                                                                                                                                        |
|------------|------------------------------------------------------------------------------------------------------------------------------------------------|
| 2026-04-25 | Renamed `spec/21-golang-email-reader/` → `spec/21-app/`; folded `spec/22-fyne-ui/plan.md` → `legacy/plan-fyne-ui.md`.                          |
| 2026-04-25 | Authored 7 features × 5 files = 35 feature spec files (Tasks #07–31).                                                                         |
| 2026-04-25 | Authored `spec/23-app-database/` (7 files, Task #32) and `spec/24-app-design-system-and-ui/` (8 files, Task #33).                              |
| 2026-04-25 | Migrated 8 solved issues from `.lovable/solved-issues/` → `spec/21-app/03-issues/solved/`; closed Watch OI-1 / OI-2 (Task #34).               |
| 2026-04-25 | Rewrote `97-acceptance-criteria.md` (v1.0 → v2.0): 35 AC-PROJ rows + 700-criterion roll-up + sign-off ladder (Task #35).                       |
| 2026-04-25 | Rewrote `99-consistency-report.md` (v1.0 → v2.0): full inventory + cross-ref validation + open-issues table + implementation deltas (Task #35). |
| 2026-04-25 | Implemented `linter-scripts/check-internal-links.sh` (AC-PROJ-33) and `linter-scripts/check-fn-length.sh` (AC-PROJ-20); both runnable, both produced real findings (links 322/322 PASS; fn-length 21 violations — see §6 Delta #8). |
| 2026-04-25 | Calibrated `check-fn-length.sh` (paren-depth tracking) and refactored `internal/ui/views/add_account_form.go` (`BuildAddAccountForm` 38 → ≤ 15) + `add_rule_form.go` (`BuildAddRuleForm` 33 → ≤ 15) by extracting widget construction, submit handlers, and message formatters into focused helpers. Linter now reports **12 real violations** (down from 21). |
| 2026-04-25 | Refactored `internal/cli/cli.go:runAdd` (30 → ≤ 15) by extracting `addAccountDefaults` struct + `resolveAddDefaults` / `promptAddIdentity` / `promptAddServer` helpers. Linter now reports **11 real violations**. |
| 2026-04-25 | Refactored `internal/watcher/watcher.go`: `Run` 21 → ≤ 15 (extracted `logStartupBanner` + `pollState` struct + `logPollError` + `handlePollOK`) and `pollOnce` 26 → ≤ 15 (extracted `connectAndSelect`, `handleBaseline`, `fetchAndCheckEmpty`, `processBatch`, `processMessage`, `persistMessage`, `evaluateRules`, `launchMatches`, `finalizeBatch`, `loadWatchState`, `logPollDone`, `advanceCursor`). Linter now reports **9 real violations**. |
| 2026-04-25 | Refactored `internal/core/read.go:ReadEmail` (28 → ≤ 15) by extracting `loadEmailDetail`, `ensureSeededRule`, `buildEngineAndLauncher`, `rowToMessage`, `evaluateMatches`, `openMatches`, `openOneMatch`. Linter now reports **8 real violations**. |
| 2026-04-26 | Wired `core.Watch` to the real poll loop: added `internal/core/watch_factory.go` (`AccountResolver`, `RealLoopFactoryDeps`, `NewRealLoopFactory`, `realLoopFactory`, `realLoop`) adapting `watcher.Run` to the `Loop` seam. New error code `ErrWatchAccountNotFound` (ER-WCH-21412). 5 new tests in `watch_factory_test.go` covering nil-dep validation, deferred alias-miss surfacing, resolver-call cardinality, account-pointer snapshot contract, and code distinctness. CF-W1 (live `PollSeconds` channel) intentionally left nil — wires up when Settings.Subscribe is bridged through Watch. `core` / `errtrace` / `eventbus` / `watcher` packages all green under `-tags nofyne`. |
| 2026-04-26 | **App boot wiring** — `internal/ui/watch_runtime.go` (framework-agnostic, no fyne import) constructs the process-wide singleton `WatchRuntime{Watch, Bus, Store, Settings}` via `sync.Once`. `buildWatchRuntime` loads config + opens the store; `attachRuntimeServices` builds Settings, the watcher event bus, the rules engine + browser launcher, then wires `NewRealLoopFactory` with an `AccountResolver` that re-reads the cached config under RWMutex. `startSettingsBridge` subscribes to Settings ONCE and (a) updates a live `PollSeconds()` accessor, (b) fans `BrowserOverride` into `launcher.Reload` (CF-T1 for the GUI, mirroring the CLI's `startReloadBridges`). Closers stack drains LIFO via `Close()` (idempotent + error-tolerant). `internal/ui/app.go:Run()` defers `watchRuntimeSingleton.Close()`; `viewFor(NavWatch)` threads `rt.Watch` + `rt.PollSeconds` into `views.WatchOptions`. `internal/ui/views/watch.go` now ships a **real Start/Stop button**: `wireWatchHeaderControls` enables the toggle when `opts.Watch` + non-empty alias are present, drives `Watch.Start` / `Watch.Stop(5s)`, and a Subscribe goroutine re-renders the status label on every WatchEvent for that alias. Pre-wiring placeholder retained when `opts.Watch == nil` (headless tests + degraded init). 5 new unit tests in `watch_runtime_test.go` lock the PollSeconds fallback chain (live → config → default 3) and Close semantics (LIFO, idempotent, error-tolerant). All 5 packages green under `-tags nofyne`; fn-length linter PASS at **0/0 violations across 74 files**. |
| 2026-04-26 | **Watch view event consumers** — `internal/ui/views/watch_events.go` (framework-agnostic) introduces three projections from `watcher.Event`: (a) `WatchCounters` + `AccumulateCounters` + `FormatCounters` for the live footer tally (polls / newMail / matches / opens / errors), (b) `FormatRawLogLine` — exhaustive switch over every `EventKind` rendering a CLI-style `HH:MM:SS  glyph  [alias]  detail` line, (c) `EventToCard` (+ `WatchCard` / `CardTone`) filtering noise (heartbeat / baseline / poll_ok / started / stopped) and tagging meaningful events (new mail = success, rule match = info, open = success/error, poll error = error, UIDVALIDITY reset = warn). Generic `AppendBounded[T]` keeps the rolling buffers bounded (cards 50, raw log 500). `internal/ui/views/watch.go` now wires real Cards + Raw log tabs and a real footer counter via a single `subscribeWatchBus` goroutine that drains `opts.Bus.Subscribe()`, alias-filters, and dispatches to three refresh callbacks. Empty-state placeholders retained when `opts.Bus == nil || opts.Alias == ""` so headless tests and pre-wiring callers stay green. `internal/ui/app.go` threads `rt.Bus` into `views.WatchOptions`. **9 new unit tests** in `watch_events_test.go` lock the kind-coverage table (counter folds, raw-log lines, card filter + tones, nil-Message defensive path, bounded buffer LIFO + cap=0 no-op, truncURL UTF-8 byte-length contract). All 16 packages green under `-tags nofyne`; fn-length linter PASS at **0/0 violations across 75 files**. |
| 2026-04-26 | **CF-W1 in-loop wiring** — Settings cadence updates now apply to a LIVE watcher without Stop+Start. `internal/core/poll_chans.go` introduces `PollChanRegistry` (Acquire / Release / Broadcast / Len) backed by `sync.RWMutex` over `map[alias]chan int` with **buffered cap=1 + drop-stale-on-full** semantics so Settings publishes stay O(N runners) and bounded; `trySend` drains the stale value before pushing the latest, guaranteeing the freshest cadence wins under burst publish. `PollChanProvider` interface added to `internal/core/watch_factory.go`; `RealLoopFactoryDeps` gains optional `PollChans`; `realLoop.Run` Acquires per-alias on entry and `defer`s Release after `watcher.Run` returns — exactly the lifetime the registry needs to stop publishing into a dead channel. `internal/ui/watch_runtime.go` constructs the singleton `PollChanRegistry`, wires it into `RealLoopFactoryDeps.PollChans`, and `forwardSettingsEvents` (extracted from `startSettingsBridge` to keep the parent under cap) now calls `Broadcast(int(ev.Snapshot.PollSeconds))` on every Settings event. **9 new race-tested unit tests** in `poll_chans_test.go` lock: read-only chan API, same-alias channel reuse (so Stop+Start overlap doesn't lose updates), alias isolation, delivery count, no-subs no-op, drop-stale-on-full (latest-wins), non-blocking under 100 undrained channels (sub-1s), Release removes slot, Release-unknown is no-op, and a concurrent Acquire+Release+Broadcast stress harness (8 aliases × 200 iters under `-race`). 15 internal packages green under `-tags nofyne -race`; fn-length linter PASS at **0/0 violations across 76 files**. |
| 2026-04-26 | **OFL font assets shipped + linter-script vet warning fixed.** (a) Dropped the actual `internal/ui/theme/fonts/Inter-Variable.ttf` (Inter v4.1, 860 KB, 19-table TrueType) and `JetBrainsMono-Variable.ttf` (JetBrains Mono v2.304, 296 KB, 20-table TrueType) — both are official upstream OFL releases pulled from `github.com/rsms/inter` and `github.com/JetBrains/JetBrainsMono`. The pre-existing `embed.FS` glob in `fonts.go` picks them up automatically; `AppTheme.Font(...)` now returns the embedded resources instead of nil-falling-back to Fyne's bundled font. The §3 typography spec is now fully realised end-to-end. **New test `Test_Fonts_PresentAndValidTTF`** (in `internal/ui/theme/sizes_test.go`) table-drives both fonts through a TrueType-magic check (`0x00010000`/`OTTO`) with `t.Skip` fallback so the existing `Test_Fonts_FallbackWhenAbsent` contract still holds on a hypothetical fresh checkout — both branches of the embed-or-nil contract are now locked. The fallback test was also strengthened to assert non-empty `mono.Content()` to mirror the existing Inter check. (b) Fixed `linter-scripts/validate-guidelines.go:738` (`fmt.Println` arg list ends with redundant newline) — the trailing `\n` inside the format string is what `Println` itself appends, so the literal `\n` was duplicated. `nix run nixpkgs#go -- vet -tags nofyne ./...` is now **silent across the entire repo** (was 1 warning). All 16 packages green under `-tags nofyne`; fn-length linter PASS at **0/0 violations across 76 files**. |
| 2026-04-26 | **Delta #1 activated end-to-end** — first production caller of `Tools.RecentOpenedUrls` lands as the new **"Recent opens"** sub-tool tab in the Tools view. `internal/ui/views/tools_recent_opens_format.go` (framework-agnostic) introduces the pure helpers: `RecentOpensFilter`, `OriginChoices` (`["All","manual","rule","cli"]` with `"All"` mapping to empty Origin), `ParseRecentOpensLimit` (clamps to [1,1000], default 100), `BuildRecentOpensSpec` (form → `core.OpenedUrlListSpec` with trim + sentinel-collapse), `FormatRecentOpensRow` (Delta-#1 columns Alias / Origin / RuleName / IsDeduped / IsIncognito rendered as `YYYY-MM-DD HH:MM:SS  [alias/origin] rule=…  (deduped,incognito)  url`), and `FormatRecentOpensSummary` (`N row(s) — alias=… origin=… limit=N (took Xms)`). `internal/ui/views/tools_recent_opens.go` (under `!nofyne`) wires alias entry + origin select + limit entry + Refresh button to `tools.RecentOpenedUrls(ctx, BuildRecentOpensSpec(form))`, then renders rows + summary + elapsed wall-clock. Tab inserted in `tools.go` after OpenUrl. **4 new table-driven tests** in `tools_recent_opens_format_test.go` cover: `ParseRecentOpensLimit` (8 inputs incl. negative + over-cap), `BuildRecentOpensSpec` (5 cases incl. "All" sentinel + alias trim), `FormatRecentOpensRow` (3 cases incl. flag suffix + em-dash fallback), `FormatRecentOpensSummary` (with + without filters). The Delta #1 SQL clauses (`AND Alias = ?` / `AND Origin = ?` in `buildOpenedUrlsQuery`) now have a real production caller; previous test-only coverage upgraded to user-reachable feature. All 16 packages green under `-tags nofyne`; fn-length linter PASS at **0/0 violations across 81 files** (`BuildRecentOpensTab` initially measured 18 stmts → split into `newRecentOpensInputs` + `newRecentOpensOutput` + `buildRecentOpensHeader` to land under cap). |
| 2026-04-26 | **Dashboard live wiring** — dashboard now renders a 5-tile live counter row (Polls / New mail / Rule matches / Opens / Errors) that subscribes to the same `watcher.Bus` the Watch view consumes. `internal/ui/views/dashboard_counters.go` (framework-agnostic) introduces `DashboardAcceptsEvent` (alias filter; empty alias = all-aliases aggregate, dashboard-only behaviour), `FormatDashboardCounterTile`, and `FormatDashboardCounterScope` (caption `Live counters — alias=… / all aliases`). `dashboard.go` adds `Bus *watcher.Bus` to `DashboardOptions`, `liveTiles` struct + `applyCounters` (reuses `WatchCounters` from slice #6 so the dashboard tiles cannot drift from the Watch footer), `newDashboardLiveRow` (renders muted placeholder when Bus is nil so headless boot stays green), and `subscribeDashboardBus` goroutine (drains Bus, alias-filters, accumulates, pushes to tile labels; closes when Bus channel closes). `internal/ui/app.go:viewFor(NavDashboard)` threads `rt.Bus` from the runtime singleton. **3 new unit tests** in `dashboard_counters_test.go` cover: `DashboardAcceptsEvent` (5 cases incl. empty-alias-all-pass + selected-alias-rejects-mismatch + selected-rejects-empty), `FormatDashboardCounterTile` (zero + non-zero), `FormatDashboardCounterScope` (with + without alias). All 16 packages green under `-tags nofyne`; fn-length linter PASS at **0/0 violations across 82 files**. |
| 2026-04-26 | **CF acceptance tests — first batch** — locked 6 of the 11 spec-mandated cross-feature contracts from `spec/21-app/02-features/07-settings/99-consistency-report.md` as executable Go tests in `internal/core/cf_acceptance_openurl_test.go` + `internal/core/cf_acceptance_settings_test.go`. **CF-T2** (`Test_CF_T2_OpenUrl_SchemeRejection_AllCallers`) — single-source-of-truth chokepoint: 12 sub-tests parameterise 4 disallowed schemes (`javascript:`, `file:`, `data:`, `chrome:`) over all 3 `OpenUrlOrigin` values (manual / rule / cli); every (caller, url) pair must surface `ER-TOOL-21761` AND never call the launcher. **CF-T3 default** (`Test_CF_T3_OpenUrl_LocalhostBlocked_Default`) — 4 loopback shapes blocked: hostname, hostname-with-port, 127.0.0.1, [::1] — all return `ER-TOOL-21762`. **CF-T3 inverse** (`Test_CF_T3_OpenUrl_LocalhostAllowed_WhenOptedIn`) — flipping `AllowLocalhostUrls=true` lets loopback URLs pass; locks the boolean as the single switch. **CF-R1** (`Test_CF_R1_Rules_OpenUrl_HonorsSchemeAllowlist`) — Origin=rule for a `javascript:` URL gets the same code as Origin=manual, proving the rule path is identical to the Tools path (no per-caller bypass). **CF-W1** (`Test_CF_W1_Watch_PollReload_OnSettingsEvent`) — real `Settings.Save` → `Subscribe` event → `PollChanRegistry.Broadcast` → an `Acquired` channel receives the new cadence within 2 s; locks the bridge contract from the spec POV. **CF-A1 strengthened** (`Test_CF_A1_Save_Accounts_Untouched`) — pre-seeds an Account via `config.Save`, runs `Settings.Save` with a non-default `PollSeconds`, then `config.Load` + struct-equality (`gotAcct != seedAccount`) on every Account field; stronger than the existing presence check. **CF-A2** (`Test_CF_A2_Concurrent_Settings_Accounts_NoLoss`) — 20-iteration lock-step harness: one goroutine drives `s.Save(PollSeconds=5..14)`, another drives `AddAccount/RemoveAccount(durable)`; final state must contain BOTH a `PollSeconds∈[5,14]` AND the durable account, proving `config.WriteAtomic` serialises both writers without lost updates on disjoint top-level keys. **5 CFs deferred** to batch #2 (see next row). **Total: 21 PASS sub-tests across 7 top-level CF tests.** All 16 packages green under `-tags nofyne`; fn-length linter PASS at **0/0 violations across 82 files**. |
| 2026-04-26 | **CF acceptance tests — second batch (final 5/11 ✅).** All 11 spec-mandated cross-feature contracts now lock as executable Go tests. **CF-T1** (`internal/ui/cf_runtime_test.go::TestCF_T1_Tools_OpenUrl_RespectsNewChromePath`) — drives `forwardSettingsEvents` with a stub event channel carrying a fresh `BrowserOverride.ChromePath`; asserts `launcher.Path()` flips from `pathA` to `pathB` after the fan-out turn, proving the same seam Tools.OpenUrl consumes is updated without restart. **CF-T4** (`internal/browser/cf_t4_test.go::TestCF_T4_IncognitoArg_OverrideVsAuto`) — table-driven 8-case matrix: 5 auto-pick cases (firefox→`-private-window`; chrome/chromium/edge/brave→`--incognito`) + 3 override cases (override beats both firefox and chrome auto-picks; empty-string override falls back to auto-pick). **CF-W3** (`internal/ui/cf_runtime_test.go::TestCF_W3_Watch_LiveCadenceUpdate`) — drives `forwardSettingsEvents` and polls the live `livePoll` accessor at 10 ms intervals with a 1 s deadline; also asserts the `PollChanRegistry.Acquire("alias-w3")` channel receives the broadcast, locking the dual fan-out (accessor for new Start calls + per-runner chan for live runners). **CF-D1** (`internal/core/cf_d1_dashboard_test.go::TestCF_D1_Dashboard_AutoStartIndicator_Live`) — round-trip Settings: subscribe, capture baseline `AutoStartWatch`, Save with the value flipped, assert the resulting `SettingsEvent.Snapshot.AutoStartWatch` equals the new value within 1 s; the indicator widget reads the same Snapshot field via `formatAutoStart` and Fyne `SetText` is synchronous so the publish-side budget is the binding contract. **CF-R2** (`internal/ui/views/cf_r2_ast_guard_test.go::TestCF_R2_RulesUI_NoSchemeBypass`) — AST + token-substring scan over all `internal/ui/views/rule*.go` (excluding `_test.go`): forbidden tokens `AllowLocalhostUrls`/`AllowPrivateIpUrls`/`OpenUrlAllowedSchemes`/`javascript:`/`file:`/`data:` and a regex catching `allowedSchemes`/`allowLocalhost`/`allowPrivateIp`/`schemeBypass`-shaped identifiers; AST walk additionally fails on any matching `*ast.Ident.Name`. Hardens the spec rule that scheme allowlisting is a Settings-only knob and the Rules UI must NEVER carry a per-rule bypass. **Total batch: 8+1+1+1+1 = 12 PASS sub-tests across 5 new top-level CF tests; cumulative across both batches: 33 PASS sub-tests across 12 top-level CF tests covering 11/11 CFs.** All 16 packages green under `-tags nofyne`; fn-length linter PASS at **0/0 violations across 82 files**. |
| 2026-04-26 | **Dashboard auto-refresh on EventNewMail.** Closes the last "must-click-Refresh" UX gap on the Dashboard. New helper `internal/ui/views/dashboard_counters.go::ShouldRefreshDashboardOnEvent(ev, last, now, minInterval) (bool, time.Time)` short-circuits on every kind except `watcher.EventNewMail` and debounces consecutive triggers (default 750 ms via the new `dashboardRefreshDebounce` const) so a 50-message backfill burst collapses into a single SQL `COUNT(*)` reload while human-paced new mail feels instant. `subscribeDashboardBus` now takes the `refresh` closure (the same one bound to the manual Refresh button by `makeDashboardRefresh`) and invokes it gated on the helper; `BuildDashboard` threads `refresh` through `newDashboardLiveRow`. The headless-test path (Bus=nil) is unchanged — placeholder still renders without spawning the goroutine. **7-case table-driven test** in `dashboard_counters_test.go` covers: first-time fire, within-debounce skip, just-past-debounce fire, three non-NewMail kinds (PollOK/RuleMatch/Heartbeat) all skipped, and zero-debounce always-fire. The static "Accounts" / "Rules enabled" tiles do NOT auto-refresh from this hook — they require future `core.AccountEvent` / `core.RuleEvent` Bus integration (tracked as new open item #5). All 16 packages green under `-tags nofyne`; fn-length linter PASS at **0/0 violations across 82 files**. |
| 2026-04-26 | **Race-build sanity sweep — all 16 packages clean under `-race`.** Ran `nix run nixpkgs#go -- test -race -tags nofyne -count=1 ./...` across the entire repo: PASSED first try with no fixes required. Then re-ran `-count=10` over every concurrency-heavy test group (regex `PollChan|Bridge|Bus|Subscribe|Forward|Dashboard`) — also clean (cumulative 4.6 s in `internal/core` and 5.2 s in `internal/watcher` under heavy stress). Confirms every primitive landed since slice #5 is race-correct: `BridgeWatcherBus` (10-kind translator), `forwardSettingsEvents` (Settings → cadence accessor + PollChans broadcast + launcher.Reload fan-out), `PollChanRegistry` (RWMutex + drop-stale buffered chans, already had its own concurrent stress test), `subscribeDashboardBus` (alias-filter + accumulate + refresh debounce), and the `RealLoopFactory` resolver closure. **No code changes** — the bridges were already designed with race-safety in mind (RWMutex around shared maps, send-only / receive-only channel directions, idempotent closers). Codified `-race` as the recommended verification step in `mem://go-verification-path` for any future goroutine / channel / shared-state change. fn-length linter: **0/0 across 82 files** (unchanged). |
| 2026-04-26 | **CF coverage extension — 6 informational CF tests (settings UpdatedAt + Tools cache E2E).** Two new test files lock cross-feature contracts called out in the original 99-consistency-report.md as worth regression-locking but not in the original 11-row CF matrix. **`internal/core/cf_acceptance_settings_extra_test.go`** introduces a `newSettingsWithClock(t, tickFn)` helper and three tests: **CF-S-MONO advances** (`TestCF_S_Mono_UpdatedAt_AdvancesWithClock`) — two saves with a strictly later injected clock produce strictly later `UpdatedAt`; **CF-S-MONO stable** (`TestCF_S_Mono_UpdatedAt_StableAtSameInstant`) — two saves at a fixed clock produce IDENTICAL `UpdatedAt` (locks the contract that UpdatedAt is sourced purely from `s.clock`, never from a hidden monotonic side-channel); **CF-S-EVENT-MONO** (`TestCF_S_EventMono_PublishedSnapshotMatchesGet`) — `SettingsEvent.Snapshot.UpdatedAt` byte-equals the value the next `Get()` returns within 1 s, so subscribers see the same as-of time as direct readers. **`internal/core/cf_acceptance_tools_invalidate_test.go`** adds three end-to-end CFs that go beyond the existing unit-level `tools_invalidate_test.go` by driving the PUBLIC `AddAccount`/`RemoveAccount` API through the real `accountBus` into a subscribed `Tools.WatchAccountEvents`: **CF-AT1** (`TestCF_AT1_RemoveAccount_EvictsToolsCache`) — `RemoveAccount(alias)` triggers eviction within 1 s; **CF-AT2** (`TestCF_AT2_ResaveAccount_EvictsToolsCache`) — re-saving an existing alias with a different `ImapHost` (which the existing AddAccount handler classifies as `AccountUpdated`) triggers eviction within 1 s; **CF-AT3** (`TestCF_AT3_AddAccount_NewAlias_DoesNotEvictOthers`) — adding a brand-new alias never evicts an unrelated cached entry (over-eviction would waste a 60 s diagnose cache cycle on every account add). Helpers `pollEvicted`, `addAccountInput`, `saveAndGetUpdatedAt`, and `newSettingsWithClock` keep the test bodies under the 15-statement linter cap. **Two informational CFs deferred** because no code seam exists yet: OpenedUrls retention vacuum (would need `internal/store/vacuum.go` + Watch tick) and Watch backoff jitter (would need exponential-backoff state in `internal/watcher/watcher.go`); both reclassified as **implementation slices** in §6 rather than test-only deltas. **Cumulative CF coverage: 39 PASS sub-tests across 18 top-level tests covering 11 mandatory + 6 informational CFs.** All 16 packages green under `-tags nofyne -race`; fn-length linter PASS at **0/0 violations across 84 files**. |
---

## 8. Final Sign-off Checklist (spec authoring)

- [x] Every feature folder has all 5 required files.
- [x] `spec/23-app-database/` and `spec/24-app-design-system-and-ui/` are authored.
- [x] Every solved issue is migrated to `03-issues/solved/` with normalized frontmatter.
- [x] `pending/` is empty (or every entry has a tracking link).
- [x] Watch OI-1 and OI-2 are closed and cross-referenced.
- [x] Project-wide `97-acceptance-criteria.md` v2.0.0 enumerates 35 AC-PROJ rows and indexes 700 total criteria.
- [x] Project-wide `99-consistency-report.md` v2.0.0 (this file) lists every spec file and audits every cross-spec reference.
- [x] Tasklist `.lovable/memory/workflow/02-spec-21-app-tasklist.md` reflects #35 as the final task.

> The spec authoring round is **complete**. Implementation work tracked under §6 is the next phase and lives outside this consistency report.

---

## 9. Validation History

| Date       | Version | Action                                                                                                                                                                                    |
|------------|---------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 2026-04-25 | 1.0.0   | Initial scaffolding consistency report (Task #06).                                                                                                                                        |
| 2026-04-25 | 2.0.0   | **Full rewrite** (Task #35). Final inventory of 71 active files; closed OI-1/OI-2; audited every error-code, named-query, and forward-reference; recorded 7 known implementation deltas.   |
| 2026-04-26 | 2.1.0   | **Settings view scaffold shipped.** Added `NavSettings` (7th sidebar entry — sidebar test updated to match) + `internal/ui/views/settings.go` (theme dropdown, poll-interval entry, Chrome-path picker, Density toggle wired through `theme.SetDensity`). Pure helpers split into `settings_logic.go` (`ParsePollSeconds`, `DensityLabelFor` / `ParseDensityChoice`, `ProjectSettingsInput`) with `settings_logic_test.go` covering 4 cases (bounds, density round-trip, invariant preservation, theme fallback). Save / Reset wired to `core.Settings.Save` / `ResetToDefaults`; status line surfaces validation + persistence errors. Read-only Config / Data / Email-archive paths panel surfaced beneath the form. 16 packages green under `-tags nofyne`; fn-length linter still **0/0** (78 files). |
| 2026-04-26 | 2.2.0   | **`watcher.Bus` → `core.WatchEvent` bridge shipped** (closes the last "follow-up slice" on Delta #3). New `internal/core/watch_bridge.go`: `BridgeWatcherBus(ctx, src, dst)` spawns ONE goroutine that drains a `*watcher.Bus` subscription and republishes onto `*eventbus.Bus[WatchEvent]` via the pure `TranslateWatcherEvent(ev) (WatchEvent, bool)`. Translation table: `EventStarted`/`EventStopped` → drop (avoid double-publish — `core.Watch.Start`/`Stop` already emit `WatchStart`/`WatchStop`); `EventPollError` → `WatchError` (carrying `Err` verbatim); `EventPollOK`/`EventBaseline`/`EventHeartbeat`/`EventNewMail`/`EventRuleMatch`/`EventUrlOpened`/`EventUidValReset` → `WatchHeartbeat` with a short human-readable `Message` (rule-match preserves the rule name). Nil-tolerant on either bus pointer; stop func is idempotent and safe in `defer`. **Wiring:** `internal/ui/watch_runtime.go::attachWatchAndBridge` constructs the destination `eventbus.Bus[core.WatchEvent]`, builds `core.Watch` against it, then calls `BridgeWatcherBus(ctx, rt.Bus, dstBus)` and registers the stop func with the runtime's closers — so a single `core.Watch.Subscribe()` now returns the unified lifecycle + runtime stream documented in spec/21-app/02-features/05-watch/01-backend.md §4. Refactored `attachRuntimeServices` into 3 helpers (`attachRuntimeServices` / `buildLoopFactory` / `attachWatchAndBridge`) to stay under the 15-statement cap. **Tests (8 new in `watch_bridge_test.go`):** `Test_TranslateWatcherEvent_Mapping` (10-row table covering every `watcher.EventKind`), `Test_TranslateWatcherEvent_PreservesError` (`errors.Is` round-trip on `Err`), `Test_TranslateWatcherEvent_PreservesAt` (timestamp survives), `Test_TranslateWatcherEvent_RuleMatchMessageHasName` (rule name appears in `Message`), `Test_BridgeWatcherBus_NilTolerant` (3 nil-combo no-ops), `Test_BridgeWatcherBus_EndToEnd` (real `watcher.Bus` + `eventbus.Bus`, 2-event sequence), `Test_BridgeWatcherBus_StopReleasesGoroutine` (`SubscriberCount` returns to 0 within 500 ms after stop), `Test_BridgeWatcherBus_DropsLifecycleDuplicates` (proves no double-publish boundary), `Test_BridgeWatcherBus_ConcurrentPublishersSafe` (2 goroutines × 50 publishes under `-race`). Also updated stale "follow-up slice" comment in `watch.go`. **Verification:** 16 packages green under `nix run nixpkgs#go -- vet ./... && go test -tags nofyne -race ./internal/core ./internal/ui ./internal/eventbus ./internal/watcher`; fn-length linter still **0/0** (79 files). |
